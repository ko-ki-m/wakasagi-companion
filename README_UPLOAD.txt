アップロード内容

GitHubリポジトリのトップ階層に lake_autofill.js を上書きアップロードしてください。

対象:
  /wakasagi-companion/lake_autofill.js

触らない:
  index.html
  app.js
  viewer/
  Pico Wスケッチ

変更点:
  1. 「魚探水深」扱いをやめ、実体は「水深」として扱う
  2. 水深は、釣行開始直後に底取りした深度値を最優先で入れる
  3. 既存viewer互換のため、water_depth_m と fishfinder_depth_m の両方に同じ水深値を保存する
  4. 開始直後深度が無い旧データだけ、同地点履歴→近隣履歴→最新履歴→未登録の順で補完する
  5. ライン・シンカー・天気・風の自動入力は維持する
  6. 既存値は上書きしない。ただし「未登録」「取得不可」「0」「0.0」は仮値として実測値で置換する
